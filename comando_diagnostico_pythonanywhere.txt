# COMANDO DE DIAGNÓSTICO PARA PYTHONANYWHERE
# Copie e cole este comando no console do PythonAnywhere

cd ~/doccollab && source venv/bin/activate && echo "🔍 DIAGNÓSTICO COMPLETO DO PYTHONANYWHERE" && echo "======================================" && echo "1. Verificando diretório atual:" && pwd && echo "2. Verificando arquivos essenciais:" && ls -la app.py wsgi.py requirements.txt babel.cfg 2>/dev/null || echo "❌ Alguns arquivos essenciais não encontrados" && echo "3. Verificando ambiente virtual:" && python --version && echo "4. Verificando dependências:" && pip list | grep -E "(flask|requests|babel)" && echo "5. Verificando traduções:" && ls -la translations/ 2>/dev/null || echo "❌ Diretório translations não encontrado" && echo "6. Testando importações básicas:" && python -c "import flask; print('✅ Flask OK')" 2>/dev/null || echo "❌ Flask erro" && python -c "import requests; print('✅ Requests OK')" 2>/dev/null || echo "❌ Requests erro" && python -c "import flask_babel; print('✅ Babel OK')" 2>/dev/null || echo "❌ Babel erro" && echo "7. Testando aplicação:" && python -c "from app import create_app; app, socketio = create_app(); print('✅ App OK')" 2>/dev/null || echo "❌ App erro" && echo "8. Verificando logs de erro:" && tail -10 /var/log/123nilmarcastro.pythonanywhere.com.error.log 2>/dev/null || echo "❌ Log de erro não encontrado" && echo "======================================" && echo "✅ DIAGNÓSTICO CONCLUÍDO!"
